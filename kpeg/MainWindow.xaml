<Window x:Class="kpeg.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kpeg"
        mc:Ignorable="d"
        Title="MainWindow" Height="590" Width="960"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:res="clr-namespace:kpeg.Properties"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}" ResizeMode="NoResize" WindowStyle="None">
    <!--<Grid>
        <Border Height="30" VerticalAlignment="Top">
            <Grid>
                <Image Source="pack://application:,,,/Resources/top.png" MouseDown="Image_MouseDown"/>
                <Button VerticalAlignment="Top" HorizontalAlignment="Right" Width="30" Height="30" BorderBrush="{x:Null}" Foreground="{x:Null}" Click="closeClick">
                    <Button.Background>
                        <ImageBrush ImageSource="/Resources/exitbutton.png"/>
                    </Button.Background>
                </Button>
                <Button VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,0,30,0" Width="30" Height="30" BorderBrush="{x:Null}" Foreground="{x:Null}" Click="minimizeClick">
                    <Button.Background>
                        <ImageBrush ImageSource="/Resources/minimizebutton.png"/>
                    </Button.Background>
                </Button>
                <Button VerticalAlignment="Top" HorizontalAlignment="Left" Width="95" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="{x:Null}">
                    
                </Button>
            </Grid>
        </Border>
        <Border Margin="0,30,0,0">
            <Grid>
                <TextBox Width="400" Height="30" Name="textBox1" TextAlignment="Center" TextChanged="textBox_TextChanged" VerticalAlignment="Top" Margin="0,10,0,0"/>
                <TextBox Width="340" Height="30" Name="textBox2" TextAlignment="Center" TextChanged="textBox2_TextChanged" VerticalAlignment="Top" Margin="0,50,60,0"/>
                <Button Width="60" Padding="0,0,0,0" VerticalAlignment="Top" Margin="350,50,0,0">Browse</Button>
                <Button Margin="100,0,100,300">Download</Button>
            </Grid>
        </Border>
    </Grid>-->
</Window>